- name: "Configure server"
  hosts: server
  remote_user: "{{as_user}}"
  become: yes
  become_method: sudo
  vars_files:
   - vars/default.yml
  roles:
    - role: users
      tags:
       - configure
       - users

